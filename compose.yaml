services:
  beacon26:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  beacon26-ai:
    build:
      context: ./ai
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    env_file:
      - ./ai/.env
